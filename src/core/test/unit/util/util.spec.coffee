describe 'Util', ->

	beforeEach ->
		module('sp.core')

	# beforeEach ->
	# 	$inject

	describe 'url function', ->
		it 'adds the configured prefix to passed in urls', ->
			expect(true).toEqual(false)
